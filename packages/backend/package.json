{
  "name": "@vmd/backend",
  "version": "1.0.0",
  "description": "VMDHub Backend - Next.js API",
  "scripts": {
    "dev": "next dev -p 3001",
    "start": "next start -p 3001",
    "build": "next build",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "@vmd/shared": "*",
    "next": "^14.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.14.0"
  }
}
