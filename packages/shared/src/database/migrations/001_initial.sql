-- Migration 001: Initial schema
-- This migration is handled by schema.sql on first run
-- Kept for reference and manual migration tracking

SELECT 'Migration 001 - Initial schema applied via schema.sql';
