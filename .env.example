# No environment variables required for Google Drive backup.
# The backup folder path is saved via the UI and stored in data/backup-config.json.
#
# To use: open the Backup modal in the sidebar, paste your local Google Drive folder
# path (synced by the Google Drive desktop app), then click Start Backup.
